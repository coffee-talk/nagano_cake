<h2>
  商品一覧
  <span>
		(全<%= @products.count %>件)
	</span>
</h2>

<% @products.each do |product| %>
  <div><%= attachment_image_tag product, :image %></div>
  <div><%= product.name %></div>
<div><%= product.price %></div>
<% end %>

<div>
  <%= paginate @products %>
</div>