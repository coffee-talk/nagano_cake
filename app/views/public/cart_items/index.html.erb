<h2>ショッピングカート</h2>

<% if @cart_items.present? %>
 <%= link_to "カートを空にする", cart_items_all_destroy_path, method: :delete %>
  <% @cart_items.each do |item| %>
  <img height="100" src="<%= item.image %>">
  <%= item.name %>
  <%= item.price %>
  <% end %>
  <%= button_to 'カートを空にする', @carted, method: :delete, data: {confirm: 'カートを空にして本当によろしいですか？'}, class:" btn btn-warning" %>
  <%= link_to "購入手続きをする", '/procedure', class:"btn btn-danger" %>
<% else %>
  <h5>カートは空です</h5>
</div>
<% end %>